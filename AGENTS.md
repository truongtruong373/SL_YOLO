# Project instructions

- Run all Python scripts, checks, and tests with the project virtual environment interpreter: `/home/truong/truongtd/bin/python`.
- Do not use the system `python` or `python3` executables for this project.
- Run project commands from `/home/truong/SL_YOLO` unless a task requires a different working directory.
